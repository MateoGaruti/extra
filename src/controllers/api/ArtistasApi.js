const {Router} = require("express");
const router = Router("");





/*
router.get ("/artistas/api", async (req,res)=>{
    const artistas = await Artista.findAll();
    res.json(artistas);
});*/

module.exports = router